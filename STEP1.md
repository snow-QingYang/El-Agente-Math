# STEP 1: Set up tooling and workspace skeleton

## Changes

- Updated root `pyproject.toml` with ruff and mypy configuration
- Created `packages/agent/` package skeleton with pyproject.toml, src layout
- Created `packages/data/` package skeleton with pyproject.toml, src layout
- Created `packages/benchmark/` package skeleton with pyproject.toml, src layout
- Added `py.typed` markers to all packages
