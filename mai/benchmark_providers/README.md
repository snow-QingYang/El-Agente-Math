to use DeepseekAgent you must first clone https://github.com/project-numina/kimina-lean-server locally and follow its read me.

Then run the kimina-lean-server docker container using docker compose up in its project root.